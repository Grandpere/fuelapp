# Sprint 27 - Execution plan

1. Align the front list screens on a smaller shared interaction contract instead of continuing one-off list behaviors.
2. Remove the remaining inline receipt creation shortcuts from list views where full-page navigation is now the clearer default.
3. Simplify the stations list so it behaves like a worklist rather than a mini-dashboard.
4. Use the sprint to absorb only small consistency fixes that appear naturally while validating the main lists.
