# Sprint 30 - Execution plan

1. Make admin maintenance details behave like a support investigation flow instead of isolated read-only screens.
2. Tighten the import-to-receipt journey so support can jump between the linked records without dead ends.
3. Harmonize the admin list screens that were improved over multiple sprints so density, labels, and action placement feel consistent.
4. Keep the implementation split into three reviewable commits and reserve one grouped validation/fix pass for the end.
