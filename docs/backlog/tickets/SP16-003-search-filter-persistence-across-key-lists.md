# SP16-003 - Search/filter persistence across key lists

## Why
Users lose context too easily after opening a detail, editing an item, or returning from a terminal flow.

## Scope
- persist key filters/sort state across the most used front/admin lists
- reduce avoidable reset-to-default moments after list actions
