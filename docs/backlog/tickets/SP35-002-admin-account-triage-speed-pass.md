# SP35-002 - Admin account triage speed pass

## Summary
Make the admin user/account workspace faster to scan so support can spot and open the next account issue without scanning every row manually.

## Delivered scope
- add a `Signal` column to the admin user list
- sort the queue by support severity instead of leaving it flat
- add top triage shortcuts to the next missing identity, unverified, and inactive account
