{% extends 'admin/layout.html.twig' %}

{% block title %}{{ 'vehicle.form.title_edit'|trans }}{% endblock %}

{% set admin_section = 'vehicles' %}

{% block admin_content %}
    <section class="card admin-panel">
        <div class="admin-page-header">
            <h2 class="page-title">{{ 'vehicle.form.title_edit'|trans }}</h2>
            <p class="muted">{{ 'admin.vehicles.form.lead'|trans }}</p>
        </div>

        {% if errors is not empty %}
            <div class="flash flash-error admin-detail-stack">
                <ul class="flash-list">
                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <form method="post" action="" data-turbo-frame="_top" class="form-stack content-narrow admin-detail-stack">
            <input type="hidden" name="_token" value="{{ csrfToken }}">
            <input type="hidden" name="_return_to" value="{{ returnTo }}">
            <label class="label">
                {{ 'admin.vehicles.form.owner_id'|trans }}
                <input class="input" type="text" name="ownerId" value="{{ formData.ownerId }}" required>
            </label>
            <label class="label">
                {{ 'vehicle.form.name'|trans }}
                <input class="input" type="text" name="name" value="{{ formData.name }}" required>
            </label>
            <label class="label">
                {{ 'vehicle.form.plate_number'|trans }}
                <input class="input" type="text" name="plateNumber" value="{{ formData.plateNumber }}" required>
            </label>

            <div class="actions-wrap">
                <button class="btn btn-primary" type="submit">{{ 'vehicle.form.action_save'|trans }}</button>
                <a class="btn btn-secondary" href="{{ returnTo }}" data-turbo-frame="_top">{{ 'common.back'|trans }}</a>
            </div>
        </form>
    </section>
{% endblock %}
