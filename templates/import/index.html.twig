{% extends 'base.html.twig' %}

{% block title %}{{ 'import.index.title'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .import-signal {
            display: grid;
            gap: 0.2rem;
            min-width: 210px;
        }

        .import-signal strong {
            font-size: 0.9rem;
        }

        .import-signal span {
            color: var(--text-soft);
            font-size: 0.82rem;
        }

        .import-quick-filters {
            display: flex;
            flex-wrap: wrap;
            gap: 0.6rem;
        }

        .import-quick-filters .pill {
            text-decoration: none;
        }

        .import-follow-up {
            display: flex;
            flex-wrap: wrap;
            gap: 0.75rem;
            align-items: center;
            justify-content: space-between;
        }

        .import-follow-up .actions-wrap {
            margin-top: 0;
        }
    </style>
{% endblock %}

{% block body %}
    {% set uploadSummaries = app.flashes('import_summary') %}
    {% set uploadSummary = uploadSummaries|length > 0 ? uploadSummaries|last : null %}
    {% set returnTo = app.request.requestUri %}
    <main class="app-shell page-stack">
        <header class="page-header-split">
            <div class="page-header">
                <h1 class="page-title">{{ 'import.index.title'|trans }}</h1>
                <p class="muted section-lead">{{ 'import.index.lead'|trans }}</p>
            </div>
            <div class="page-actions">
                <a class="btn btn-secondary" href="{{ path('ui_receipt_index') }}" data-turbo-frame="_top">{{ 'nav.receipts'|trans }}</a>
                <a class="btn btn-secondary" href="{{ path('ui_analytics_dashboard') }}" data-turbo-frame="_top">{{ 'nav.analytics'|trans }}</a>
            </div>
        </header>

        <section id="import-upload-card" class="card section-card">
            <form method="post" enctype="multipart/form-data" data-turbo-frame="_top" class="form-stack content-narrow">
                <input type="hidden" name="_token" value="{{ csrf_token('ui_import_upload') }}">
                <label class="label">
                    {{ 'import.index.upload_label'|trans }}
                    <input class="input" type="file" name="files[]" accept=".pdf,.zip,image/jpeg,image/png,image/webp" multiple required>
                </label>
                <p class="muted section-lead">
                    {{ 'import.index.upload_limits'|trans }}
                </p>
                <div class="actions-wrap">
                    <button class="btn btn-primary" type="submit">{{ 'import.index.upload_submit'|trans }}</button>
                    <a class="btn btn-secondary" href="{{ path('ui_import_index') }}" data-turbo-frame="_top">{{ 'import.index.refresh'|trans }}</a>
                </div>
            </form>
        </section>

        {% if uploadSummary is iterable %}
            <section class="card section-card">
                <div class="section-header">
                    <div>
                        <h2 class="section-title">{{ 'import.index.last_upload_summary'|trans }}</h2>
                        <p class="muted section-lead">
                            {{ 'import.index.last_upload_counts'|trans({'%accepted%': uploadSummary.acceptedCount, '%rejected%': uploadSummary.rejectedCount}) }}
                        </p>
                    </div>
                </div>

                {% if uploadSummary.accepted|length > 0 %}
                    <div class="form-stack">
                        <p class="text-block-title">{{ 'import.index.accepted'|trans }}</p>
                        <ul class="flash-list">
                            {% for accepted in uploadSummary.accepted %}
                                <li class="flash">
                                    <strong>{{ accepted.filename }}</strong>
                                    {% if accepted.source != accepted.filename %}
                                        <span class="muted">({{ accepted.source }})</span>
                                    {% endif %}
                                    <span class="muted">{{ 'import.index.accepted_item'|trans({'%status%': accepted.status}) }}</span>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}

                {% if uploadSummary.rejected|length > 0 %}
                    <div class="form-stack">
                        <p class="text-block-title">{{ 'import.index.rejected'|trans }}</p>
                        <ul class="flash-list">
                            {% for rejected in uploadSummary.rejected %}
                                <li class="flash flash-error">
                                    <strong>{{ rejected.filename }}</strong>
                                    {% if rejected.source != rejected.filename %}
                                        <span class="muted">({{ rejected.source }})</span>
                                    {% endif %}
                                    <span>{{ rejected.reason }}</span>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}
            </section>
        {% endif %}

        <section class="card section-card">
            <div class="section-header">
                <div>
                    <h2 class="section-title">{{ 'import.index.my_jobs'|trans }}</h2>
                    <p class="muted section-lead">{{ 'import.index.my_jobs_lead'|trans }}</p>
                </div>
            </div>

            <div class="form-stack">
                <div class="import-quick-filters">
                    {% for filter in statusQuickFilters %}
                        <a
                            class="pill {{ filter.isActive ? 'pill-date' : '' }}"
                            href="{{ filter.url }}"
                            data-turbo-frame="_top"
                        >
                            {{ filter.label }}: {{ filter.count }}
                        </a>
                    {% endfor %}
                </div>

                {% if followUpShortcuts|length > 0 %}
                    <div class="card section-card import-follow-up">
                        <div>
                            <p class="text-block-title">{{ 'import.index.follow_up_now'|trans }}</p>
                            <p class="muted section-lead">
                                {% if statusFilter %}
                                    {{ 'import.index.filtered_on'|trans({'%status%': statusLabels[statusFilter] ?? statusFilter})|raw }}
                                {% else %}
                                    {{ 'import.index.follow_up_default'|trans }}
                                {% endif %}
                            </p>
                            <p class="muted section-lead">{{ 'import.index.follow_up_hint'|trans }}</p>
                        </div>
                        <div class="actions-wrap">
                            {% for shortcut in followUpShortcuts %}
                                <a class="btn btn-{{ shortcut.variant }} btn-compact" href="{{ shortcut.url }}" data-turbo-frame="_top">{{ shortcut.label }}</a>
                            {% endfor %}
                        </div>
                    </div>
                {% endif %}
            </div>

            <div class="table-wrap">
                <table class="app-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>{{ 'import.index.col_status'|trans }}</th>
                            <th>{{ 'import.index.col_filename'|trans }}</th>
                            <th>MIME</th>
                            <th>{{ 'import.index.col_size'|trans }}</th>
                            <th>{{ 'import.index.col_created'|trans }}</th>
                            <th>{{ 'import.index.col_updated'|trans }}</th>
                            <th>{{ 'import.index.col_signal'|trans }}</th>
                            <th>{{ 'import.index.col_action'|trans }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for row in jobs %}
                            {% set job = row.job %}
                            <tr
                                class="table-row row-link"
                                data-controller="row-link"
                                data-action="click->row-link#navigate"
                                data-row-link-url-value="{{ path('ui_import_show', {id: job.id.toString, return_to: returnTo}) }}"
                                onclick="if (!event.target.closest('a,button,input,form,select,textarea,label,[data-row-link-ignore]')) { window.location.assign(this.getAttribute('data-row-link-url-value')); }"
                            >
                                <td>{{ job.id.toString }}</td>
                                <td>
                                    <span class="pill status-pill status-{{ job.status.value }}">
                                        {{ statusLabels[job.status.value] ?? job.status.value }}
                                    </span>
                                </td>
                                <td>{{ job.originalFilename }}</td>
                                <td>{{ job.mimeType }}</td>
                                <td>{{ job.fileSizeBytes }} bytes</td>
                                <td>{{ job.createdAt|date('Y-m-d H:i:s') }}</td>
                                <td>{{ job.updatedAt|date('Y-m-d H:i:s') }}</td>
                                <td>
                                    <div class="import-signal">
                                        <strong>{{ row.summary.headline }}</strong>
                                        <span>{{ row.summary.detail }}</span>
                                    </div>
                                </td>
                                <td class="action-cell">
                                    <div class="action-row" data-row-link-ignore>
                                        <a class="btn btn-{{ row.primaryAction.variant }} btn-compact" href="{{ row.primaryAction.url }}" data-turbo-frame="_top">{{ row.primaryAction.label }}</a>
                                        {% if row.secondaryAction %}
                                            <a class="btn btn-{{ row.secondaryAction.variant }} btn-compact" href="{{ row.secondaryAction.url }}" data-turbo-frame="_top">{{ row.secondaryAction.label }}</a>
                                        {% endif %}
                                        <form method="post" action="{{ path('ui_import_delete', {id: job.id.toString}) }}" data-turbo-frame="_top" onsubmit="return confirm('{{ 'import.confirm.delete'|trans|e('js') }}');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('ui_import_delete_' ~ job.id.toString) }}">
                                            <input type="hidden" name="_redirect" value="{{ returnTo }}">
                                            <button class="btn btn-danger btn-compact" type="submit">{{ 'action.delete'|trans }}</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="9" class="muted">{{ 'import.index.empty'|trans }}</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </section>
    </main>
{% endblock %}
